import JXG from "jsxgraph"

JXG.Options.circle = {
    ...JXG.Options.circle,
    showinfobox: "inherit"
}

JXG.Options.line = {
    ...JXG.Options.line,
    showinfobox: "inherit"
}

JXG.Options.arc = {
    ...JXG.Options.arc,
    showinfobox: "inherit"
}