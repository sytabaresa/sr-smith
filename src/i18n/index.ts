import en from "./translations.en.json"
import es from "./translations.es.json"

const i18n = {
    translations: {
        en,
        es,
    },
    defaultLang: "en",
    useBrowserDefault: true,
};

export default i18n
